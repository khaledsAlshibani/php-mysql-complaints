# Server Configuration
HOST=localhost
PORT=8000

# Application URLs
# Leave PORT empty to use Apache/Nginx default port
# For Apache: APP_URL=http://localhost/php-mysql-complaints-app/src/
# For PHP built-in server: APP_URL will be auto-generated from HOST:PORT
APP_URL=

# Database Configuration (matching docker-compose.yml)
DB_HOST=localhost
DB_NAME=complaints_suggestions_db
DB_USER=root
DB_PASS=root